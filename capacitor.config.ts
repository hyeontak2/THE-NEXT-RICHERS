import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.thenextrichers.app",
  appName: "THE NEXT RICHERS",
  webDir: "out",
};

export default config;
